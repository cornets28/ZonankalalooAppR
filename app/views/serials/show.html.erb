<% provide(:title, "Serie") %>

<p id="notice"><%= notice %></p>



<div class="container" style="padding-top: 90px;">
    <div class="row">  
       <div class="col-sm-12">
          
           <div class="section-title text-center">
               <h2><span><%= @serial.mainTitle %><hr></span></h2>
               <img src="/img/title-bottom.png" alt="">
              
           </div>
       </div>
       <% if has_role?(:admin) %>
         <%= link_to 'Chanje Non Kategori', edit_serial_path(@serial), class:"button"%>
       <% end %>
       </div> <br />
      <%= render 'shared/scenes_by_serial' %>
  </div>
</div>


 <!--<%# @serials.each do |serial| %>   
            <div class="col-sm-12">
                <div class="col-sm-12">
                    <div class="section-title text-center">
                        <h2><span><%#= serial.mainTitle %><hr></span></h2>
                        <img src="img/title-bottom.png" alt="">
                    </div>
                    <%# if serial.scenes.any? %>
                        <div class="donate-button ptb-32">
                            <%#= link_to "Plis Scene Nan Serie Sa",  serial_path(id: serial.id), class:"waves-effect waves-light" %>
                            <span class="icon is-small"><i class="fa fa-hand-o-right" aria-hidden="true"></i></span>              
                        </div>
                        <%#= link_to "Plis Atik Nan Kategori Sa",  channel_path(id: serial.id) %>  <%#This one ##############%>
                    <%# end %>
                </div>
         <%# end %>-->